# 运行环境设置
spring:
  profiles:
    active: prod
  servlet:
    multipart:
      max-file-size: 10MB
  jackson:
    default-property-inclusion: non_null   # 序列化时默认排除值为null的字段
logging:
  level:
    com.cystrix: WARN
